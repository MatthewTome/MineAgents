import { describe, it, expect } from "vitest";
import * as subject from "../../../src/actions/handlers/mining.js";

describe("actions/handlers/mining.ts", () => {
  it("exports something", () => {
    expect(subject).toBeDefined();
  });
});